# CurrenSee 💰

**CurrenSee** is a Flutter-based currency companion app that provides real-time exchange rates, currency conversion, rate alerts, market news, and a full user account system — all wrapped in a clean gold-themed UI with dark mode support.

> 4th Semester E-Project

---

## Tech Stack

| Layer | Technology |
|---|---|
| Framework | Flutter (Dart) |
| Authentication | Firebase Auth — Email/Password & Google Sign-In |
| Database | Cloud Firestore |
| Notifications | Firebase Cloud Messaging (FCM) + flutter_local_notifications |
| State Management | Provider |
| HTTP | http package |
| Local Storage | shared_preferences |
| PDF Export | pdf + printing |

---

## Features

### 🔐 Authentication
- Email/Password Sign Up & Sign In
- Google Sign-In
- Onboarding screen shown only on first launch
- Auth state persistence — auto-login on relaunch

### 💱 Currency Tools
- **Currency Converter** — Convert between currencies in real time
- **Currency List** — Browse all supported currencies
- **Exchange Rate Information** — View live rates at a glance
- **Conversion History** — Track past conversions

### 🔔 Rate Alerts
- Automated background rate checks every **15 minutes**
- Local push notifications when rates change for: USD, EUR, GBP, SAR, AED, CNY (vs PKR)
- Toggleable via master switch and per-feature toggle in User Preferences

### 📰 News & Trends
- Currency News and Market Trends screen
- News Detail view with full article content
- URL launcher for external sources

### 👤 User Account
- Profile screen with editable display name and photo (via image picker)
- Edit Profile and Change Password screens
- Firestore-backed user data

### 💬 Community
- Feedback submission (stored in Firestore)
- All Feedbacks / Reviews screen (read community feedback)

### ⚙️ Settings & More
- **Dark / Light Mode** toggle (persisted via Provider + ThemeMode)
- User Preferences screen
- Notifications management screen
- User Support & Help Center
- PDF export support (printing package)

---

## Project Structure (high level)

```
lib/
├── main.dart                   # App entry, Firebase init, FCM setup, rate check service
├── firebase_options.dart       # FlutterFire generated config
├── Constants/
│   └── Constants.dart          # Brand colors / app constants
├── theme/
│   ├── App_Theme.dart        # Light and dark ThemeData
│   └── Theme_Provider.dart   # ChangeNotifier for theme switching
├── Module/
│   ├── currency_model.dart
│   └── NewsArticleModel.dart
└── Screens/
    ├── on_screen.dart                        # Onboarding
    ├── Login_Screen.dart                     # Sign In
    ├── Register_Screen.dart                  # Sign Up
    ├── Home_Screen.dart                      # Main scaffold (drawer + bottom nav)
    ├── HomeTab_Screen.dart                   # Home tab content
    ├── CurrencyConverter_Screen.dart
    ├── Currency_ListScreen.dart
    ├── ExchangeRate_InformationScreen.dart
    ├── CurrencyHistory_Screen.dart
    ├── RateAlerts_Screen.dart
    ├── CurrencyNews_MarketTrends_Screen.dart
    ├── NewsDetail_Screen.dart
    ├── AppNotifications_Screen.dart
    ├── UserPreferences_Screen.dart
    ├── UserSupport_HelpCenter_Screen.dart
    ├── FeedBack_Screen.dart
    ├── AllFeedbacks_Screen.dart
    ├── Profile_Screen.dart
    ├── EditProfile_Screen.dart
    └── Changepassword_Screen.dart
```

---

## Dependencies (pubspec.yaml)

- firebase_core
- firebase_auth
- cloud_firestore
- provider
- http
- pdf
- printing
- intl
- shared_preferences
- url_launcher
- google_sign_in
- firebase_messaging
- permission_handler
- flutter_local_notifications
- cupertino_icons
- flutter_launcher_icons

---

## Installation

### 1) Prerequisites
- Install **Flutter SDK** (Flutter & Dart)
- Android Studio (for Android builds) or Xcode (for iOS builds)
- A Firebase project set up for this app

### 2) Get packages
From the project root:

```bash
flutter pub get
```

---

## Firebase Setup

### 1) Create Firebase project
- Enable **Firebase Authentication**
  - Email/Password
  - Google Sign-In
- Enable **Cloud Firestore**
- Enable **Firebase Cloud Messaging**

### 2) Add FlutterFire configuration
Your project includes `lib/firebase_options.dart` (generated by the FlutterFire CLI).

If you need to re-generate it:

```bash
flutterfire configure
```

---

## Running the App

```bash
flutter run
```

---

## Currency API

Live exchange rates are fetched from a free, open-source API (no API key required):

```
https://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@latest/v1/currencies/pkr.json
```

---

## Notifications (FCM + Local Alerts)

The app uses two notification layers:

| Type | Usage |
|---|---|
| **FCM (Firebase Cloud Messaging)** | Remote push notifications |
| **flutter_local_notifications** | Local rate-change alerts triggered in-app/background |

Rate alerts fire automatically every **15 minutes** via a background rate-check workflow (using stored snapshots in `shared_preferences`).

Android notification delivery uses `high_importance_channel`.

---

## Authentication Rules

### Sign Up — Password Requirements
Password must contain:
- At least **8 characters**
- At least one **uppercase letter** (A–Z)
- At least one **lowercase letter** (a–z)
- At least one **number** (0–9)
- At least one **special character** (`!@#$%^&*` etc.)

### Sign In — Error Messaging
User-friendly messages are shown for common failures, such as:
- Invalid email format
- Wrong email or password
- Disabled account
- Too many attempts
- No internet connection
- Google Sign-In cancelled

---

## Running Tests

```bash
flutter test
```

---

## Test Credentials

> ⚠️ Warning: Remove or replace test credentials before making this repository public.

| Account | Email | Password | Auth Method |
|---|---|---|---|
| Test User 1 | user1@gmail.com | Us@12345678 | Email/Password |
| Google Account | Any Gmail | Any Gmail | Google Sign-In |

---

## App Theme

The app uses a custom gold brand color: `#C7A729`.

- Light & dark themes are defined in:
  - `lib/theme/App_Theme.dart`
- Theme switching is handled by:
  - `lib/theme/Theme_Provider.dart`

---

## Notes / Troubleshooting

- If Firebase auth or Firestore is not working:
  - Ensure `google-services.json` (Android) and iOS `GoogleService-Info.plist` (if applicable) are present in the correct locations.
  - Re-check enabled providers in Firebase Console.
- If notifications do not appear:
  - Verify Android notification permission (`permission_handler`).
  - Confirm FCM is configured correctly for the app.

